#include "media/srt.ceu"

var&? Scene s = spawn Scene(Size(640,480),true);
watching s do
    await Srt(&s.scene,
              "resources/slideshow.srt",
              Region(0, 400, 3, 640, 80),
          );
end

escape 0;
